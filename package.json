{
    "name": "rideon-mern-app",
    "version": "1.0.0",
    "description": "RideOn MERN Application",
    "scripts": {
        "start": "node backend/server.js",
        "heroku-postbuild": "npm --prefix frontend install && npm --prefix frontend run build"
    },
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "dotenv": "^16.4.5"
    }
}